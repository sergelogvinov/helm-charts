- name: fluent-bit-static.rules
  rules:
  - alert: FluentBitOutputError
    expr: rate(fluentbit_output_errors_total{app_kubernetes_io_name="fluent-bit"}[5m]) > 0
    for: 2m
    labels:
      severity: warning
    annotations:
      description: In the last 5 minutes, fluent-bit output error increased. Current rate is {{ $value }}
      summary: FluentBit output are failing
  - alert: FluentBitRecordsDropped
    expr: rate(fluentbit_output_dropped_records_total{app_kubernetes_io_name="fluent-bit"}[5m]) > 0
    for: 2m
    labels:
      severity: warning
    annotations:
      description: In the last 5 minutes, fluent-bit output dropped increased. Current rate is {{ $value }}
      summary: FluentBit output are failing
